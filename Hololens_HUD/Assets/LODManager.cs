using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LODManager : MonoBehaviour
{
    // Start is called before the first frame update
    [SerializeField]
    GameObject[] InterfaceTemplate;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
